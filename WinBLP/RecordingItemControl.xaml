<UserControl x:Class="BatRecordingManager.RecordingItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BatRecordingManager"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="500">
    
    <StackPanel Orientation="Vertical" x:Name="RecordingStackPanel" Background="{StaticResource WindowBackgroundBrush}">

        <Label

            Style="{StaticResource SimpleLabel}"
            x:Name="LineOneLabel" BorderThickness="0,2,0,0" BorderBrush="Black" />
        <Label

            Style="{StaticResource SimpleLabel}"
            x:Name="LineTwoLabel" />
        <Label
            Style="{StaticResource SimpleLabel}"

            x:Name="LineThreeLabel" />
        <StackPanel x:Name="BatPassSummaryStackPanel" Orientation="Vertical" />
        <ListView
            x:Name="LabelledSegmentListView" Style="{StaticResource SimpleListBox}"/>
    </StackPanel>
</UserControl>